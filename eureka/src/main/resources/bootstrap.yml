spring:
  application:
    name: eureka
  cloud:
    config:
      uri: http://localhost:8888/
      profile: dev
      label: master
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:8902/eureka/

server:
  port: 8901